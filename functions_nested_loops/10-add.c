#include <stdio.h>
#include "main.h"
/**
 * add - A function that adds two integers and returns the result.
 * @a: It's number.
 * @b: It's number.
 * Return: return c in end.
 */

int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
